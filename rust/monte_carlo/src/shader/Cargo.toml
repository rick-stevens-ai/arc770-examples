[package]
name = "shader"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["dylib"]

[dependencies]
spirv-std = "0.9"
glam = "0.24"
rand_xoshiro = { version = "0.6", default-features = false }

[build-dependencies]
spirv-builder = "0.9"
